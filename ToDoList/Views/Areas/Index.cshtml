@model IEnumerable<Dados.Area>

@{
    ViewBag.Title = "Index";
}

<h2>Areas</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codarea }) |
            @Html.ActionLink("Details", "Details", new { id=item.codarea }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codarea })
        </td>
    </tr>
}

</table>
