@model IEnumerable<Dados.Funcionario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.datanasc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Funcao1.tipo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.datanasc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Funcao1.tipo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codfuncionario }) |
            @Html.ActionLink("Details", "Details", new { id=item.codfuncionario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codfuncionario })
        </td>
    </tr>
}

</table>
