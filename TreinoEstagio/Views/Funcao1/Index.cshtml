@model IEnumerable<TreinoEstagio.Dados.Funcao1>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.salario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.salario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codfuncao }) |
            @Html.ActionLink("Details", "Details", new { id=item.codfuncao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codfuncao })
        </td>
    </tr>
}

</table>
